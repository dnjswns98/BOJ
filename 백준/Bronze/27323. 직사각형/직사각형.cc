#include <iostream>
using namespace std;
#define max 1000001

int arr[max] = { 0 };

int main() {
	cin.tie(NULL);
	ios::sync_with_stdio(false);
	
	int a, b;
	cin >> a >> b;
	cout << a * b;
}
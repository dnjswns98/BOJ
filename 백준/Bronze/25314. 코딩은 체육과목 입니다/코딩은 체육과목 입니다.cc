#include <iostream>
using namespace std;


int main() {
	cin.tie(NULL);
	ios::sync_with_stdio(false);
	long long n, m, k;

	cin >> n;
	for (int i = 0; i < n/4; i++) {
		cout << "long ";
	}
	cout << "int";
}